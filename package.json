{
  "name": "kratos-runtime-resolver",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf ./index.{js,d.ts}",
    "compile": "swc index.ts --out-file ./index.js -s",
    "build": "npm run prebuild && npm run compile && tsc",
    "prepack": "npm run build",
    "test": "npm run prebuild && npm run compile && mocha ./test/**/*.spec.ts",
    "coverage": "npm run prebuild && npm run compile && nyc mocha ./test/**/*.spec.ts"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "kratos-core": "^1.1.2",
    "node-fetch": "2.6.6"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.46",
    "@swc/helpers": "^0.5.0",
    "@types/chai": "^4.3.4",
    "@types/fs-extra": "^11.0.1",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0",
    "regenerator-runtime": "^0.13.11",
    "rimraf": "^4.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  }
}
